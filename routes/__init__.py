# This file tells Python that the 'routes' directory is a package.
# It can also be used to gather all routers for easier import in main.py.

from . import auth
from . import issues
from . import erp_metadata # New import for ERP metadata router
